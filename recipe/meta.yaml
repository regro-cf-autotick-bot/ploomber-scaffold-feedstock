{% set name = "ploomber-scaffold" %}
{% set version = "0.2.5" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ploomber-scaffold-{{ version }}.tar.gz
  sha256: a417a666e10f86b0596ea4f0cbc800a3d7450b500a1392bdde79522efe19f660

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6
    - jinja2

test:
  imports:
    - ploomber_scaffold
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/ploomber/ploomber
  summary: Supporting package for ploomber
  license: Apache-2.0
  license_file: LICENSE
  dev_url: https://github.com/ploomber/ploomber

extra:
  recipe-maintainers:
    - edublancas
    - RoyalTS
